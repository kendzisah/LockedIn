/**
 * Profile tab — unified Settings screen (see `features/settings/screens/SettingsScreen.tsx`).
 */

import SettingsScreen from '../../settings/screens/SettingsScreen';

export default SettingsScreen;
